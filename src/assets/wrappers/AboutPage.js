import styled from "styled-components";

const Wrapper = styled.div`
  h3 {
    color: var(--grey-500);
  }
  p {
    line-height: 2;
    color: var(--grey-500);
    margin-top: 2rem;
  }
`;

export default Wrapper;
